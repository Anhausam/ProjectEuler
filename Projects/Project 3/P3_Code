#Project Euler
#Project 3: Largest Prime Factor
#Asher Hausam

print("---PROGRAM START---")

number = 600851475143

divisor = 2

biggestPrimeDivisor = 0
while(divisor <= number):
	while(number % divisor == 0):
		print(divisor)
		number = (number / divisor)
		biggestPrimeDivisor = divisor
	divisor += 1

print("ANS:" + str(biggestPrimeDivisor))

input("---PROGRAM END---")
